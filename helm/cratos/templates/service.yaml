apiVersion: v1
kind: Service
metadata:
  name: {{ include "cratos.fullname" . }}
  labels:
    {{- include "cratos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cratos.selectorLabels" . | nindent 4 }}
