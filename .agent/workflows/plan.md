---
name: plan
description: PM 에이전트를 통한 계획 수립
triggers:
  - "/plan"
  - "계획 세워줘"
---

# /plan - 계획 수립

## 입력

- 자연어 요청
- 컨텍스트 (현재 프로젝트 상태)

## 출력

- .agent/plan.json
- 실행 계획 요약

## 프로세스

### 1. 요청 분석

```
입력: "이 이슈 고쳐서 PR 만들어줘"

분석:
- 목표: Issue 해결 + PR 생성
- 범위: 코드 수정, 테스트, 커밋
- 필요 에이전트: rust-agent, qa-agent, commit
```

### 2. 작업 분해

```json
{
  "goal": "Issue #123 해결 및 PR 생성",
  "tasks": [
    {
      "id": "analyze",
      "agent": "rust-agent",
      "action": "이슈 분석 및 코드 탐색",
      "depends_on": []
    },
    {
      "id": "fix",
      "agent": "rust-agent",
      "action": "코드 수정",
      "depends_on": ["analyze"]
    },
    {
      "id": "test",
      "agent": "qa-agent",
      "action": "테스트 실행",
      "depends_on": ["fix"]
    },
    {
      "id": "commit",
      "agent": "commit",
      "action": "PR 생성",
      "depends_on": ["test"]
    }
  ]
}
```

### 3. 위험도 평가

```
위험도: Write

이유:
- 파일 수정 포함
- Git 작업 포함
- 외부 API 호출 (GitHub)
```

### 4. 승인 요청

```
승인 모드에 따라:
- always: 사용자 확인 요청
- risky_only: Write 이상이므로 확인 요청
- never: 바로 실행
```

## 출력 예시

```markdown
## 📋 실행 계획

### 목표
Issue #123 해결 및 PR 생성

### 단계
1. [rust-agent] 이슈 분석 및 코드 탐색
2. [rust-agent] 코드 수정
3. [qa-agent] 테스트 실행
4. [commit] PR 생성

### 위험도
🟡 Write (파일 수정, Git 작업 포함)

### 예상 시간
약 5-10분

[▶️ 실행] [✏️ 수정] [❌ 취소]
```
