---
name: develop
description: 원격 개발지시 E2E 워크플로우
triggers:
  - "/develop"
  - "이슈 고쳐줘"
  - "PR 만들어줘"
  - "버그 수정해줘"
---

# /develop - 원격 개발지시

## 7단계 워크플로우

### Step 1: 요청 분석

```
입력: "이 GitHub 이슈 #123 해결해서 PR 만들어줘"

분석:
├── 목표: Issue #123 해결
├── 범위: 코드 수정 + 테스트 + PR
├── 필요 도구: git, file, github
└── 위험도: Write
```

### Step 2: 레포 준비

```bash
# Clone (필요시)
git clone https://github.com/user/repo.git
cd repo

# Fetch & Branch
git fetch origin
git checkout -b fix/issue-123 origin/main
```

### Step 3: 코드 탐색

```
1. 이슈 분석
   - GitHub API로 이슈 내용 조회
   - 관련 파일 식별

2. 코드 검색
   - Serena find_symbol 사용
   - 영향 범위 파악
```

### Step 4: 수정

```
1. 패치 생성
   - 변경 내용 결정
   - diff 생성

2. 패치 적용
   - file_write 도구 사용
```

### Step 5: 검증

```bash
cargo test
cargo clippy -- -D warnings
cargo fmt -- --check
```

### Step 6: 커밋 & PR

```bash
# Commit
git add -A
git commit -m "fix: resolve issue #123

- Added null check in handler
- Updated tests

Co-Authored-By: Claude <noreply@anthropic.com>"

# Push & PR
git push -u origin fix/issue-123
gh pr create --title "Fix #123: Add null check" --body "..."
```

### Step 7: 리포트

```
┌─────────────────────────────────────────────────────────┐
│ 📊 개발 작업 완료 리포트                                │
├─────────────────────────────────────────────────────────┤
│ 목표: Issue #123 - Null pointer exception 수정          │
│                                                         │
│ 변경 요약:                                              │
│ ├── src/handler.rs: +5 -2 (null check 추가)            │
│ └── tests/handler_test.rs: +12 (테스트 추가)           │
│                                                         │
│ 검증 결과:                                              │
│ ├── cargo test: ✅ 54 passed                           │
│ ├── cargo clippy: ✅ 0 warnings                        │
│ └── cargo fmt: ✅ no changes                           │
│                                                         │
│ 산출물:                                                 │
│ └── PR #456: https://github.com/user/repo/pull/456     │
└─────────────────────────────────────────────────────────┘
```

## 권한 부족 시 대체 경로

```
권한이 없으면:
1. 필요한 권한 안내
2. PR 생성 직전까지 준비
3. diff/커밋 내용 제공

"여기까지 완료했습니다. 푸시는 직접 실행해주세요:
git push -u origin fix/issue-123"
```
