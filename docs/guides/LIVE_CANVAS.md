# Live Canvas - 실시간 시각적 워크스페이스

## 개요

Live Canvas는 AI 에이전트가 작업 결과를 실시간으로 시각화하고, 사용자와 인터랙티브하게 협업할 수 있는 공간입니다. 자율 에이전트(Autonomous Agent)의 ReAct 루프가 Plan-Act-Reflect 원칙에 따라 23개 내장 도구(코드 실행, 파일 조작, `image_generate` 등)를 자율적으로 선택하여 캔버스 콘텐츠를 생성합니다.

### 핵심 기능

| 기능 | 설명 |
|------|------|
| **실시간 스트리밍** | LLM 응답을 청크 단위로 즉시 표시 |
| **멀티모달 렌더링** | 코드, 다이어그램, 이미지(`image_generate` 도구 연동), 차트 통합 표시 |
| **양방향 편집** | 사용자가 캔버스 내용을 직접 수정 가능 |
| **버전 관리** | 변경 히스토리 추적 및 되돌리기 |
| **협업 지원** | 멀티 커서, 동시 편집 |

## 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                     Live Canvas UI                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ Code Panel  │  │  Diagram    │  │   Preview Panel     │  │
│  │ (Monaco)    │  │  (Mermaid)  │  │   (Rendered HTML)   │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
                   WebSocket (실시간)
                           │
┌─────────────────────────────────────────────────────────────┐
│                    Canvas Server (Rust)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ Session Mgr │  │  Document   │  │   Renderer Engine   │  │
│  │ (tokio)     │  │  Store      │  │   (markdown/code)   │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────────┐
│                    Cratos Core                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ Orchestrator│  │    LLM      │  │      Replay         │  │
│  │             │  │  (Streaming)│  │    (History)        │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 기술 스택

### Backend (Rust)

```toml
# crates/cratos-canvas/Cargo.toml
[dependencies]
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }

# 실시간 통신
tokio-tungstenite = "0.21"
futures = "0.3"

# 문서 처리
pulldown-cmark = "0.10"     # Markdown 파싱
syntect = "5"               # 코드 하이라이팅
```

### Frontend

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "@monaco-editor/react": "^4.6.0",
    "mermaid": "^10.6.0",
    "katex": "^0.16.0",
    "y-websocket": "^1.5.0",
    "yjs": "^13.6.0"
  }
}
```

## 핵심 타입

```rust
/// 캔버스 세션
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CanvasSession {
    pub id: Uuid,
    pub user_id: String,
    pub document: CanvasDocument,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}

/// 캔버스 문서
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CanvasDocument {
    pub blocks: Vec<CanvasBlock>,
    pub version: u64,
}

/// 캔버스 블록 타입
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(tag = "type")]
pub enum CanvasBlock {
    /// Markdown 텍스트
    Markdown {
        id: Uuid,
        content: String,
    },
    /// 코드 블록
    Code {
        id: Uuid,
        language: String,
        content: String,
        executable: bool,
    },
    /// Mermaid 다이어그램
    Diagram {
        id: Uuid,
        diagram_type: DiagramType,
        source: String,
    },
    /// 이미지
    Image {
        id: Uuid,
        url: String,
        alt: String,
    },
    /// 차트 (Chart.js/D3)
    Chart {
        id: Uuid,
        chart_type: ChartType,
        data: serde_json::Value,
    },
}

/// 다이어그램 타입
#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum DiagramType {
    Flowchart,
    Sequence,
    ClassDiagram,
    StateDiagram,
    EntityRelationship,
    Gantt,
}
```

## WebSocket 프로토콜

### 클라이언트 → 서버

```rust
#[derive(Debug, Serialize, Deserialize)]
#[serde(tag = "type")]
pub enum ClientMessage {
    /// 세션 참여
    Join { session_id: Uuid },

    /// 블록 업데이트
    UpdateBlock {
        block_id: Uuid,
        content: String,
        cursor_position: Option<usize>,
    },

    /// 블록 추가
    AddBlock {
        after_id: Option<Uuid>,
        block: CanvasBlock,
    },

    /// 블록 삭제
    DeleteBlock { block_id: Uuid },

    /// 블록 이동
    MoveBlock {
        block_id: Uuid,
        new_position: usize,
    },

    /// AI에게 요청
    AskAI {
        prompt: String,
        context_blocks: Vec<Uuid>,
    },
}
```

### 서버 → 클라이언트

```rust
#[derive(Debug, Serialize, Deserialize)]
#[serde(tag = "type")]
pub enum ServerMessage {
    /// 세션 상태
    SessionState { document: CanvasDocument },

    /// 블록 업데이트 (다른 사용자 또는 AI)
    BlockUpdated {
        block_id: Uuid,
        content: String,
        source: UpdateSource,
    },

    /// AI 스트리밍 응답
    AIStreaming {
        block_id: Uuid,
        chunk: String,
        is_complete: bool,
    },

    /// 에러
    Error { message: String },

    /// 커서 위치 (협업)
    CursorUpdate {
        user_id: String,
        block_id: Uuid,
        position: usize,
    },
}

#[derive(Debug, Serialize, Deserialize)]
pub enum UpdateSource {
    User(String),
    AI,
    System,
}
```

## 사용 예시

### 1. 코드 생성 및 실행

```
[사용자] "Rust로 피보나치 함수 만들어줘"

[AI 캔버스에 코드 블록 추가]
┌─────────────────────────────────────────┐
│ ```rust                                  │
│ fn fibonacci(n: u64) -> u64 {           │
│     match n {                            │
│         0 => 0,                          │
│         1 => 1,                          │
│         _ => fibonacci(n-1) + fib...    │ ← 실시간 스트리밍
│     }                                    │
│ }                                        │
│ ```                                      │
│ [▶ 실행] [📋 복사]                        │
└─────────────────────────────────────────┘
```

### 2. 다이어그램 생성

```
[사용자] "이 시스템 아키텍처 다이어그램으로 그려줘"

[AI 캔버스에 Mermaid 다이어그램 추가]
┌─────────────────────────────────────────┐
│ ┌─────────┐     ┌─────────┐             │
│ │  User   │────▶│   API   │             │
│ └─────────┘     └────┬────┘             │
│                      │                   │
│               ┌──────┴──────┐           │
│               ▼             ▼           │
│          ┌─────────┐  ┌─────────┐       │
│          │   LLM   │  │   DB    │       │
│          └─────────┘  └─────────┘       │
│ [🔧 편집] [📥 다운로드 PNG]               │
└─────────────────────────────────────────┘
```

### 3. 협업 편집

```
[사용자 A 편집 중]
┌─────────────────────────────────────────┐
│ # 프로젝트 계획                          │
│                                          │
│ ## 1단계: 설계|                          │ ← 사용자 A 커서
│ - 요구사항 분석      [🟢 사용자 B]       │ ← 사용자 B 커서
│ - 아키텍처 설계                          │
│                                          │
│ [🤖 AI가 3단계 추가 중...]                │
└─────────────────────────────────────────┘
```

## Replay 연동

Live Canvas는 Cratos의 Replay 시스템과 통합되어 모든 변경사항을 추적합니다:

```rust
/// Canvas 이벤트 (Replay 저장용)
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CanvasEvent {
    pub session_id: Uuid,
    pub timestamp: DateTime<Utc>,
    pub event_type: CanvasEventType,
    pub actor: ActorType,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum CanvasEventType {
    BlockAdded(CanvasBlock),
    BlockUpdated { block_id: Uuid, old: String, new: String },
    BlockDeleted(Uuid),
    BlockMoved { block_id: Uuid, from: usize, to: usize },
    AIRequestStarted { prompt: String },
    AIResponseCompleted { block_id: Uuid },
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum ActorType {
    User(String),
    AI(String),  // 모델명
}
```

### 타임라인 보기

```
[Canvas Timeline]
┌────────────────────────────────────────────────────────────┐
│ 10:00:00 │ 👤 User   │ 새 문서 생성                         │
│ 10:00:15 │ 👤 User   │ "피보나치 함수 만들어줘" 요청         │
│ 10:00:16 │ 🤖 AI     │ 코드 블록 추가 시작                   │
│ 10:00:18 │ 🤖 AI     │ 코드 블록 완료 (45 tokens)           │
│ 10:00:25 │ 👤 User   │ 코드 수정: "u64" → "u128"            │
│ 10:00:30 │ 👤 User   │ [▶ 실행] 클릭                        │
├──────────┴───────────┴───────────────────────────────────────┤
│ [◀ 되돌리기] [재실행 ▶] [dry-run 🔍]                          │
└────────────────────────────────────────────────────────────┘
```

## 설정

```toml
# config/default.toml
[canvas]
enabled = true
port = 8081
max_sessions = 100
max_blocks_per_document = 500

# WebSocket 설정
[canvas.websocket]
heartbeat_interval_secs = 30
max_message_size_kb = 1024

# 협업 설정
[canvas.collaboration]
enabled = true
max_users_per_session = 10
sync_interval_ms = 100

# AI 스트리밍 설정
[canvas.ai_streaming]
enabled = true
chunk_size = 20  # 토큰 단위
```

## API 엔드포인트

```
# REST API
GET  /api/canvas/sessions              # 세션 목록
POST /api/canvas/sessions              # 새 세션 생성
GET  /api/canvas/sessions/:id          # 세션 조회
DELETE /api/canvas/sessions/:id        # 세션 삭제

# WebSocket
WS   /api/canvas/ws/:session_id        # 실시간 연결

# 내보내기
GET  /api/canvas/sessions/:id/export   # Markdown/HTML 내보내기
POST /api/canvas/sessions/:id/snapshot # 스냅샷 저장
```

## 보안

- 세션별 인증 토큰 필수
- WebSocket 연결 시 JWT 검증
- 블록별 권한 관리 (읽기/쓰기)
- Rate limiting (분당 요청 제한)

## 향후 계획

1. **v1.0**: 기본 캔버스 + 코드/마크다운
2. **v1.1**: 다이어그램 지원 (Mermaid)
3. **v1.2**: 협업 기능 (Yjs)
4. **v2.0**: 코드 실행 (WebAssembly 샌드박스)
